# Model registry data

This directory contains versioned model-specific task contexts and segment-confirmation artifacts. Mesh files do not belong here; they live in `model_store/`.

In production set `BRICKSMART_MODEL_REGISTRY_ROOT` to persistent storage outside the source checkout.
