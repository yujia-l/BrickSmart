"""
KidSpark AI — Agent Pipeline
Owner: Developer B

This package contains all Pydantic AI agents. The pipeline has three phases:

  Phase 1 (Interactive): Storybook Analysis -> Teacher Consultation -> Block Awareness
  Phase 2 (Automated): Outline Planner -> Build Target -> Teacher Plan + Student Guide -> Validator

Each agent is an independent Pydantic AI Agent with a typed result_type.
The orchestrator manages transitions between phases and passes outputs forward.
"""
