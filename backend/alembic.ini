# KidSpark AI — Alembic Configuration
# Owner: Developer A
#
# This file configures Alembic for database migrations.
# The actual migration environment is in migrations/env.py.
#
# SETUP:
#   1. Update sqlalchemy.url below (or use env var)
#   2. Generate migration: alembic revision --autogenerate -m "description"
#   3. Apply migration: alembic upgrade head
#
# [alembic]
# script_location = migrations
# sqlalchemy.url = postgresql://kidspark:password@localhost:5432/kidspark
