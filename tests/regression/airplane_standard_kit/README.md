# Airplane standard-kit feasibility fixture

This fixture verifies that the reviewed full-scale airplane is rejected cleanly
when it is run against the physical standard-kit inventory.

- Inventory: `standard_kit` (finite)
- Expected status: `INFEASIBLE_INVENTORY`
- Validated final claim: no
- Build-instructions HTML: not generated

This is a normal feasibility result, not a planner crash. A separate compacted
airplane contract is required before a standard-kit airplane HTML can exist.
